해시(Hash)?
- 해시는 key-value 쌍으로 이루어져 있는 구조
- 파이썬에서는 딕셔너리를 이용하여 해시를 구현한다.
- 배열은 [] 튜플은 (), 딕셔너리는 {}
- ex )
    value  = {
        'color' : 'green',
        'name' : 'smyoo'
    }

value[color] -> color라는 키 접근
values.keys() -> 키를 전부 호출
values.values() -> 값을 전부